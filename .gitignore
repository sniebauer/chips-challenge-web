node_modules/
dist/
.src_game/
*.log
.DS_Store
tools/soundfonts/
# Provenance copy of the ripped font sheet; not used at runtime (the extractor
# reads the gitignored .src_game/ sheet). Keep only the derived font assets.
assets/fonts/source.png
